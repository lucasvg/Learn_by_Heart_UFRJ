package com.learnbyheart.greenDao.gen.dao;

import java.util.List;
import java.util.ArrayList;

import com.learnbyheart.greenDao.gen.bean.Example;
import com.learnbyheart.greenDao.gen.bean.Meaning;
import com.learnbyheart.greenDao.gen.dao.DaoSession;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.internal.DaoConfig;
import de.greenrobot.dao.query.Query;
import de.greenrobot.dao.query.QueryBuilder;


// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table EXAMPLE.
*/
public class ExampleDao extends AbstractDao<Example, Long> {

    public static final String TABLENAME = "EXAMPLE";

    /**
     * Properties of entity Example.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Example = new Property(1, String.class, "example", false, "EXAMPLE");
        public final static Property MeaningId = new Property(2, long.class, "meaningId", false, "MEANING_ID");
    };

    private DaoSession daoSession;

    private Query<Example> meaning_ExamplesQuery;

    public ExampleDao(DaoConfig config) {
        super(config);
    }
    
    public ExampleDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'EXAMPLE' (" + //
                "'_id' INTEGER PRIMARY KEY ," + // 0: id
                "'EXAMPLE' TEXT NOT NULL ," + // 1: example
                "'MEANING_ID' INTEGER NOT NULL );"); // 2: meaningId
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'EXAMPLE'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, Example entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindString(2, entity.getExample());
        stmt.bindLong(3, entity.getMeaningId());
    }

    @Override
    protected void attachEntity(Example entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public Example readEntity(Cursor cursor, int offset) {
        Example entity = new Example( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.getString(offset + 1), // example
            cursor.getLong(offset + 2) // meaningId
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, Example entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setExample(cursor.getString(offset + 1));
        entity.setMeaningId(cursor.getLong(offset + 2));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(Example entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(Example entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    /** Internal query to resolve the "examples" to-many relationship of Meaning. */
    public List<Example> _queryMeaning_Examples(long meaningId) {
        synchronized (this) {
            if (meaning_ExamplesQuery == null) {
                QueryBuilder<Example> queryBuilder = queryBuilder();
                queryBuilder.where(Properties.MeaningId.eq(null));
                meaning_ExamplesQuery = queryBuilder.build();
            }
        }
        Query<Example> query = meaning_ExamplesQuery.forCurrentThread();
        query.setParameter(0, meaningId);
        return query.list();
    }

    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getMeaningDao().getAllColumns());
            builder.append(" FROM EXAMPLE T");
            builder.append(" LEFT JOIN MEANING T0 ON T.'MEANING_ID'=T0.'_id'");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected Example loadCurrentDeep(Cursor cursor, boolean lock) {
        Example entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        Meaning meaning = loadCurrentOther(daoSession.getMeaningDao(), cursor, offset);
         if(meaning != null) {
            entity.setMeaning(meaning);
        }

        return entity;    
    }

    public Example loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<Example> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<Example> list = new ArrayList<Example>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<Example> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<Example> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
