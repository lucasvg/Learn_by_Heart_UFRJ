package com.learnbyheart.greenDao.gen.dao;

import java.util.List;
import java.util.ArrayList;

import com.learnbyheart.greenDao.gen.bean.Example;
import com.learnbyheart.greenDao.gen.bean.LogExampleNotification;
import com.learnbyheart.greenDao.gen.bean.Notification;
import com.learnbyheart.greenDao.gen.dao.DaoSession;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.internal.DaoConfig;
import de.greenrobot.dao.query.Query;
import de.greenrobot.dao.query.QueryBuilder;


// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table LOG_EXAMPLE_NOTIFICATION.
*/
public class LogExampleNotificationDao extends AbstractDao<LogExampleNotification, Long> {

    public static final String TABLENAME = "LOG_EXAMPLE_NOTIFICATION";

    /**
     * Properties of entity LogExampleNotification.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Result = new Property(1, String.class, "result", false, "RESULT");
        public final static Property Date = new Property(2, java.util.Date.class, "date", false, "DATE");
        public final static Property ExampleId = new Property(3, long.class, "exampleId", false, "EXAMPLE_ID");
        public final static Property NotificacaoId = new Property(4, long.class, "notificacaoId", false, "NOTIFICACAO_ID");
    };

    private DaoSession daoSession;

    private Query<LogExampleNotification> example_LogExampleNotificationsQuery;
    private Query<LogExampleNotification> notification_LogExampleNotificationsQuery;

    public LogExampleNotificationDao(DaoConfig config) {
        super(config);
    }
    
    public LogExampleNotificationDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'LOG_EXAMPLE_NOTIFICATION' (" + //
                "'_id' INTEGER PRIMARY KEY ," + // 0: id
                "'RESULT' TEXT," + // 1: result
                "'DATE' INTEGER," + // 2: date
                "'EXAMPLE_ID' INTEGER NOT NULL ," + // 3: exampleId
                "'NOTIFICACAO_ID' INTEGER NOT NULL );"); // 4: notificacaoId
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'LOG_EXAMPLE_NOTIFICATION'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, LogExampleNotification entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String result = entity.getResult();
        if (result != null) {
            stmt.bindString(2, result);
        }
 
        java.util.Date date = entity.getDate();
        if (date != null) {
            stmt.bindLong(3, date.getTime());
        }
        stmt.bindLong(4, entity.getExampleId());
        stmt.bindLong(5, entity.getNotificacaoId());
    }

    @Override
    protected void attachEntity(LogExampleNotification entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public LogExampleNotification readEntity(Cursor cursor, int offset) {
        LogExampleNotification entity = new LogExampleNotification( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // result
            cursor.isNull(offset + 2) ? null : new java.util.Date(cursor.getLong(offset + 2)), // date
            cursor.getLong(offset + 3), // exampleId
            cursor.getLong(offset + 4) // notificacaoId
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, LogExampleNotification entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setResult(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setDate(cursor.isNull(offset + 2) ? null : new java.util.Date(cursor.getLong(offset + 2)));
        entity.setExampleId(cursor.getLong(offset + 3));
        entity.setNotificacaoId(cursor.getLong(offset + 4));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(LogExampleNotification entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(LogExampleNotification entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    /** Internal query to resolve the "logExampleNotifications" to-many relationship of Example. */
    public List<LogExampleNotification> _queryExample_LogExampleNotifications(long exampleId) {
        synchronized (this) {
            if (example_LogExampleNotificationsQuery == null) {
                QueryBuilder<LogExampleNotification> queryBuilder = queryBuilder();
                queryBuilder.where(Properties.ExampleId.eq(null));
                example_LogExampleNotificationsQuery = queryBuilder.build();
            }
        }
        Query<LogExampleNotification> query = example_LogExampleNotificationsQuery.forCurrentThread();
        query.setParameter(0, exampleId);
        return query.list();
    }

    /** Internal query to resolve the "logExampleNotifications" to-many relationship of Notification. */
    public List<LogExampleNotification> _queryNotification_LogExampleNotifications(long notificacaoId) {
        synchronized (this) {
            if (notification_LogExampleNotificationsQuery == null) {
                QueryBuilder<LogExampleNotification> queryBuilder = queryBuilder();
                queryBuilder.where(Properties.NotificacaoId.eq(null));
                notification_LogExampleNotificationsQuery = queryBuilder.build();
            }
        }
        Query<LogExampleNotification> query = notification_LogExampleNotificationsQuery.forCurrentThread();
        query.setParameter(0, notificacaoId);
        return query.list();
    }

    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getExampleDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T1", daoSession.getNotificationDao().getAllColumns());
            builder.append(" FROM LOG_EXAMPLE_NOTIFICATION T");
            builder.append(" LEFT JOIN EXAMPLE T0 ON T.'EXAMPLE_ID'=T0.'_id'");
            builder.append(" LEFT JOIN NOTIFICATION T1 ON T.'NOTIFICACAO_ID'=T1.'_id'");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected LogExampleNotification loadCurrentDeep(Cursor cursor, boolean lock) {
        LogExampleNotification entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        Example example = loadCurrentOther(daoSession.getExampleDao(), cursor, offset);
         if(example != null) {
            entity.setExample(example);
        }
        offset += daoSession.getExampleDao().getAllColumns().length;

        Notification notification = loadCurrentOther(daoSession.getNotificationDao(), cursor, offset);
         if(notification != null) {
            entity.setNotification(notification);
        }

        return entity;    
    }

    public LogExampleNotification loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<LogExampleNotification> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<LogExampleNotification> list = new ArrayList<LogExampleNotification>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<LogExampleNotification> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<LogExampleNotification> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
